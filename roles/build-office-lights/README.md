# Build Office Lights

This role installs go (1.22), checks out the repository containing the office lights code and builds it. 
The path can be customized using the "project_path" variable, which should be an absolute path.

It can be used together with the "clone-repo" role in case a repository should be cloned first. 